#include "main.h"

/**
 * mul -  function that multiplies two integers
 * @a: parameter to check
 * @b: parameter to check
 * Return: Always 0
 */
int mul(int a, int b)
{
	int c;

	c = (a * b);
	return (c);
}
